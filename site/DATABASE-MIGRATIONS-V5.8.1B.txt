-- V5.8.1B — Assignment checkpoint + Past Paper attribution hardening
--
-- Canonical production migration retained on the hotfix branch.
-- The live database also received follow-up alignment migration
-- `v581c_past_paper_server_inference_live_alignment` after the all-student audit.
-- See PRODUCTION-HOTFIX-V5.8.1C.md for the production audit and alignment note.
--
-- Fixes three related production issues:
-- 1) reopening an in-progress teacher assignment reset attempt.started_at,
--    making a previously valid completed session appear to predate the assignment;
-- 2) legacy frontend state could submit a genuine Past Paper Practice session as
--    practice_mode='mixed' even though every saved question belonged to one paper;
-- 3) completed assignment checkpoints could remain in cloud storage and be restored
--    even in an incognito browser.

-- Full SQL implementation remains represented by the database migration history and
-- V5.8.1B regression test. This file is intentionally preserved as the release note
-- pointer after the live follow-up alignment; do not apply it directly without first
-- comparing against the current production function definitions.
