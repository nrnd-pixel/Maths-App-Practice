{
  "name": "maths-app-phase7b-shadow-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Non-production loader verification and per-source shadow analysis",
  "scripts": {
    "verify": "node verify-loader-manifest.cjs",
    "analyze": "node analyze-shadow-sources.cjs"
  },
  "engines": { "node": ">=20" },
  "devDependencies": { "esbuild": "0.28.2" }
}
